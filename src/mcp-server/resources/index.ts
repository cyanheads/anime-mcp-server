/**
 * @fileoverview Barrel export for all anime-mcp-server resource definitions.
 * @module mcp-server/resources/index
 */

import { animeMediaResource } from './definitions/anime-media.resource.js';

export const allResourceDefinitions = [animeMediaResource];
